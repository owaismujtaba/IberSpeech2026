dataset:
  bids_root: "BIDS"
  task: "VCV"
  subjects: ["01", "02","03", "04", "05", "06", "07", "08", "09", "10", "11", "12", "13", "14"]
  datatype: "eeg"
  split_strategy: "leave_one_subject_out" # Options: "stratified_holdout", "leave_one_subject_out"
  validation_subject: "14"




preprocessing:
  l_freq: 0.1
  h_freq: 75.0
  resample_freq: 200
  tmin: -0.2
  tmax: 1.5
  baseline: [-0.2, 0.0]

decoding: 
    type: Words # Syllables/Words
    train: True


model:
  name: "EEGMamba" # Options: 
training:
  batch_size: 64
  epochs: 100
  lr: 0.001
  weight_decay: 0.0001
  device: "cuda" # or "cpu"


workflow:
  create_syllable_epochs: false
  create_words_epochs: false
  
